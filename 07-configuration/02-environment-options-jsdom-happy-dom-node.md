# Environment Options

`environment: 'jsdom'` (full DOM), `'happy-dom'` (lighter), `'node'` (no DOM). Configure per file.

> 📘 Next: [include, exclude & testMatch](03-include-exclude-and-test-match.md)
