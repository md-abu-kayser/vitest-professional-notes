# Vitest CLI Cheatsheet

- `vitest` – run tests
- `vitest --watch` – watch mode
- `vitest --ui` – UI dashboard
- `vitest --coverage` – enable coverage
- `vitest --shard=1/3` – shard tests
- `vitest --reporter=verbose` – verbose output
